# SSH target for make deploy (user@host)
DEPLOY_HOST=radio@42fm.ru

# Host path to the music library (mounted read-only into the container)
MUSIC_PATH=/home/radio/content/music

# Ollama model for local LLM agent (pulled automatically on first start)
OLLAMA_MODEL=huihui_ai/qwen3.5-abliterated:9b
